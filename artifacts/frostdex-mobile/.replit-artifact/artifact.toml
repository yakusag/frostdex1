kind = "mobile"
previewPath = "/mobile/"
title = "FrostDex Mobile"
version = "1.0.0"
id = "artifacts/frostdex-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
name = "expo"
paths = [ "/mobile/" ]
localPort = 23456

[services.development]
run = "pnpm --filter @workspace/frostdex-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/frostdex-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/frostdex-mobile", "run", "serve" ]

[services.env]
PORT = "23456"
BASE_PATH = "/mobile/"

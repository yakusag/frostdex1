{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "overrides": {
      "crypto-es": "file:./lib/crypto-es-shim",
      "cli-color": "file:./lib/cli-color-stub",
      "es5-ext": "file:./lib/es5-ext-stub",
      "@solana/wallet-adapter-trezor": "file:./lib/solana-wallet-trezor-stub",
      "form-data@2.3.3": "file:./lib/form-data-stub",
      "request": "file:./lib/request-stub"
    }
  }
}
